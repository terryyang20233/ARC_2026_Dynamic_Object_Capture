Jetson deployment artifacts.

- `best.onnx` — portable ONNX export of the tennis-ball detector.
- TensorRT `.engine` files are hardware-specific and are not tracked (see `.gitignore`).
- Do not commit `nvcamtest_*.jpg` camera dumps.
